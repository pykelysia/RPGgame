#include<graphics.h>
#include "namespace.h"

int main() {

	initgraph(1280, 640);

	while (true) {
		mystd::TIME start = GetTickCount();

		mystd::TIME end = GetTickCount();
		auto delta = end - start;
		if()
	}

	closegraph();

	return 0;
}