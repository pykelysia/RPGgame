#include "../lib/std.h"
#include<graphics.h>
#include<math.h>

//mystd::TIME mystd::GetTimeCount() {
//	TIME trueTime = 0 ;
//	TIME mayTime = GetTickCount();
//	if (lastTime > mayTime) {
//		lastTime = mayTime;
//		timeCount++;
//	}
//	trueTime = ULONG_MAX * timeCount + lastTime;
//	return trueTime;
//}